export const myApiURL = 'http://127.0.0.1:8000/api';

export const serverRoutes = {
    employees: myApiURL + '/employees',
    // updateEmployee: myApiURL + '/employees/' + id + '/update',
    employeeLogin: myApiURL + '/employees/login',
}